import 'package:flutter/material.dart';

class ProfileScrren extends StatelessWidget {
  const ProfileScrren ({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Center(child: Text("hhhhhhhh")),
    );
  }
}